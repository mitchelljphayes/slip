name: Comments

on: pull_request

permissions:
  contents: read

jobs:
  words-for-humans:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: mitchelljphayes/words-for-humans@v0.2.0
        with:
          version: 0.2.0
